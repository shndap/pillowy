export async function api(path:string,options:RequestInit={}){const tg=(window as any).Telegram?.WebApp;const base=import.meta.env.VITE_API_URL||'http://localhost:8000';const r=await fetch(base+path,{...options,headers:{'Content-Type':'application/json','X-Telegram-Init-Data':tg?.initData||'',...(options.headers||{})}});if(!r.ok)throw Error(await r.text());return r.json()}
