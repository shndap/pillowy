export async function api(path:string,options:RequestInit={}){const tg=(window as any).Telegram?.WebApp;const base=import.meta.env.VITE_API_URL||'https://pillowy-api.onrender.com';const headers:any={'Content-Type':'application/json','X-Telegram-Init-Data':tg?.initData||''};try{headers['X-Timezone']=Intl.DateTimeFormat().resolvedOptions().timeZone}catch{};const r=await fetch(base+path,{...options,headers:{...headers,...(options.headers||{})}});if(!r.ok){const body=await r.text();if(r.status===409){let message='Not enough pills for this dose.';try{message=JSON.parse(body).detail||message}catch{};window.alert(message)}throw Error(body)}return r.json()}
